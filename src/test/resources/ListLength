header {
package generated
}

tokens [
    CONS = "::";
    WORD = "[a-z]";
    NIL = "Nil";
    WS = "[ \t\n\r]+"
]

skip [
    WS;
]

start = LIST;

rules [
    LIST(greeting: String) -> len: Integer ::= {println(greeting)} nil=NIL {len=0} |
        {val newGreet = greeting + " recursion"} h=WORD cons=CONS l=LIST(newGreet) {len = l + 1};;
]
