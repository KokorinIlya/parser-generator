header {
package generated
}

tokens [
    CONS = "::";
    WORD = "[a-z]";
    NIL = "Nil";
    WS = "[ \t\n\r]+"
]

skip [
    WS;
]

start = LIST;

rules [
    LIST() -> u: Unit ::= nil=NIL {println("Empty list")} |
        {println("Parsing head")} h=WORD {println(s"Head is $h")} cons=CONS {println("Cons parsed")} l=LIST() {println("Parser list")};;
]
